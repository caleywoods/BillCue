<!-- Probably move the stylesheet to you layout. Also make sure you include the javascript. -->
- stylesheet_link_tag "event_calendar"
#header-container
  %h1#header Panda Calendar 

  = image_tag('panda.png')

%p
  = link_to 'Add New Event', new_event_path, :class => 'lbOn'

= raw(event_calendar)
